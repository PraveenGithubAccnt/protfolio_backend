spring.application.name=chatBot

# Server Configuration
server.port=${PORT:8080}
server.address=0.0.0.0

gemini.api.key=${GEMINI_API_KEY:}

gemini.model=gemini-2.5-flash
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/${gemini.model}:generateContent

# ✅ CORS Configuration
cors.allowed.origins=http://localhost:8080,http://localhost:3000,https://my-portfolio-production-14d8.up.railway.app,https://praveengithubaccnt.github.io

# Logging
logging.level.com.portfolio.chatbot=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.web.cors=INFO
